spring:
  application:
    name: orders-service

  datasource:
    url: jdbc:postgresql://localhost:5433/orders
    username: orders
    password: orders

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: false
