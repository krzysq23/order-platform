apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: order-platform
  labels:
    app: orders-service
spec:
  type: ClusterIP
  selector:
    app: inventory-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
      protocol: TCP

